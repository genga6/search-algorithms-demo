# 探索アルゴリズム学習リポジトリ ― 設計・背景・ロードマップ

> 目的：**膨大な状態空間から、効率よく「それなりに良い解」を見つける**という営みを、
> 主要アルゴリズムを実装・可視化しながら体系的に理解する。
> スタック：**TypeScript + Vite**（各アルゴリズムに 1 ページのインタラクティブ・ビジュアライザ＋短い解説）。
> 深い強化学習（DQN・方策勾配）まで踏み込む場合のみ Python(torch) を別途併設。

---

## 1. このリポジトリの狙い

- **手を動かして理解する**：探索は「フロンティアがどう広がるか」を目で見るのが最短の理解ルート。
  全アルゴリズムを同じ例題の上で走らせ、挙動・解の質・計算量を横並びで比較する。
- **共通言語で捉える**：状態(state)・行動(action)・遷移(transition)・コスト(cost)・
  ヒューリスティック(heuristic)・ゴール判定(goal test)・フロンティア(open/closed) という語彙で
  すべての探索を記述する。
- **系統の地図を持つ**：情報なし探索 → 情報あり探索 → 系統的最適化 → ゲーム木 →
  局所探索 → 確率的 → 勾配 → モデルベース → 強化学習、という連なりを一枚の地図にする。

---

## 2. 全体を貫く 4 本の軸

これらの軸を意識すると、個々の手法が「どの問題を解こうとしているか」が見える。

| 軸 | 一言 | 代表する手法 |
|----|------|--------------|
| **最適性 ⇄ 計算コスト** | 最適解の保証を取るか、速さ・省メモリを取るか | BFS/UCS/A\*（保証）↔ 貪欲/ビーム/重み付きA\*（近似） |
| **探索 vs 活用 (explore/exploit)** | 未知を試すか、既知の良さを伸ばすか | バンディット/UCB, ε-greedy, 焼きなましの温度, MCTS |
| **枝刈り (pruning)** | 「見なくてよい未来」を切る | Alpha-Beta, 分枝限定法, A\*（ヒューリスティックによる枝刈り） |
| **記憶と再利用** | 一度解いた部分問題を保存して使い回す | 動的計画法(DP), 置換表(transposition table), メモ化 |

---

## 3. 系統の地図（カバーする全アルゴリズム）

```
探索空間から効率よく良い解を得る
├─ A. 情報なし探索(Uninformed)          BFS / DFS / IDDFS / UCS(Dijkstra) / 双方向
├─ B. 情報あり探索(Informed)            貪欲最良優先 / A* / 重み付きA* / IDA* / ビーム
├─ C. 動的計画法(DP)                    最短路DP / ナップサックDP / 編集距離
├─ D. 系統的最適化・枝刈り               バックトラッキング / CSP(N-Queens,数独) / 分枝限定法
├─ E. ゲーム木(Adversarial)             Minimax / Alpha-Beta / MCTS
├─ F. 局所探索・メタヒューリスティクス    山登り / 焼きなまし / タブー / 遺伝的(GA) / 粒子群(PSO)
├─ G. 確率的手法(Stochastic)            モンテカルロ(π推定) / MCMC(Metropolis)
├─ H. 勾配ベース最適化                   GD/SGD / Momentum / RMSProp / Adam
├─ I. モデルベース最適化                 ベイズ最適化(GP + 獲得関数)
└─ J. 強化学習(RL)                      多腕バンディット/UCB / Q学習 / SARSA  (発展: DQN, 方策勾配)
```

---

## 4. アーキテクチャ（共通インターフェース）

探索・最適化・ゲーム・強化学習は「解こうとしている問題の形」が本質的に違うため、
**1 つに無理に統一せず、薄い 4 つのインターフェース**で受ける。この分割自体が学びになる。

### 4.1 状態空間探索 ― A / B / C / D で共有

```ts
// 状態 S、行動 A をジェネリクスで
interface SearchProblem<S, A> {
  initial: S;
  actions(s: S): A[];
  result(s: S, a: A): S;              // 遷移
  stepCost(s: S, a: A, s2: S): number;
  isGoal(s: S): boolean;
  heuristic?(s: S): number;           // 情報あり探索用：ゴールまでの下界推定（許容的なら A* が最適）
  key(s: S): string;                  // 訪問済み(closed)判定のためのハッシュ
}

interface SearchResult<S, A> {
  found: boolean;
  path: S[];
  actions: A[];
  cost: number;
  trace: SearchEvent<S>[];            // 可視化用イベント列（push / pop / relax など）
  stats: { expanded: number; generated: number; frontierMax: number; timeMs: number };
}

type Search<S, A> = (p: SearchProblem<S, A>) => SearchResult<S, A>;
```

**可視化の要**は `trace`（イベント列）。アルゴリズムは解を計算しつつ
「いつどのノードを open に入れ／pop したか」を記録し、UI 側はそれを一定間隔で再生するだけ。
これでアルゴリズム本体と描画が疎結合になる。

### 4.2 最適化 ― F / H / I で共有

```ts
interface OptimizationProblem<X> {
  random(): X;             // ランダム初期解
  neighbor(x: X): X;       // 近傍（局所探索用）
  objective(x: X): number; // 最小化する目的関数（エネルギー）
  gradient?(x: X): X;      // 勾配（勾配法用、あれば）
}
```

### 4.3 ゲーム木 ― E で共有

```ts
interface Game<S, A> {
  initial: S;
  actions(s: S): A[];
  result(s: S, a: A): S;
  isTerminal(s: S): boolean;
  utility(s: S, player: number): number; // 終端の評価
  currentPlayer(s: S): number;
}
```

### 4.4 強化学習 / MDP ― J で共有

```ts
interface Environment<S, A> {
  reset(): S;
  step(a: A): { next: S; reward: number; done: boolean };
  actions(s: S): A[];
}
```

### 4.5 評価指標（全手法共通で計測）

- **解の質**：経路長 / 目的関数値 / 勝率 / 累積報酬
- **効率**：展開ノード数(expanded)・生成ノード数(generated)・最大フロンティア幅・実時間
- **性質**：完全性(complete)・最適性(optimal)・収束性

---

## 5. アルゴリズム・リファレンス

各表の凡例：完全性＝解があれば必ず見つけるか / 最適性＝最良解を保証するか /
`b`＝分岐数, `d`＝解の深さ, `m`＝最大深さ, `V,E`＝頂点・辺数。

### A. 情報なし探索（Uninformed）

| 手法 | 完全 | 最適 | 時間 | 空間 | デモ | 実装メモ |
|------|------|------|------|------|------|----------|
| BFS 幅優先 | ○ | ○(単位コスト) | O(b^d) | O(b^d) | グリッド迷路 | キュー。層ごとに広がる |
| DFS 深さ優先 | △ | × | O(b^m) | O(bm) | グリッド迷路 | スタック。深追いに注意 |
| IDDFS 反復深化 | ○ | ○(単位コスト) | O(b^d) | O(bd) | 深さ制限を上げる様子 | DFS を深さ上限つきで反復。BFS並の最適性を省メモリで |
| UCS / Dijkstra | ○ | ○ | O(E + V log V) | O(V) | 重み付きグリッド | 優先度キュー。確定距離が波紋状に拡大 |
| 双方向探索 | ○ | ○(条件付) | O(b^(d/2)) | O(b^(d/2)) | 2 つのフロンティアが出会う | start と goal から同時探索。交差点で連結 |

### B. 情報あり探索（Informed / ヒューリスティック）

| 手法 | 完全 | 最適 | 時間/空間 | デモ | 実装メモ |
|------|------|------|-----------|------|----------|
| 貪欲最良優先 | × | × | 最悪 O(b^m) | グリッド | `h(n)` のみで選ぶ。速いが遠回り・非最適 |
| A\* | ○ | ○(h が許容的) | 最悪 指数、実用は h 次第 | グリッド | `f=g+h`。h が良いほど展開が激減 |
| 重み付き A\* | ○ | w-最適(w≥1) | A\* より高速 | グリッド | `f=g+w·h`。w で速さ⇄最適性を調整 |
| IDA\* | ○ | ○ | O(b^d) | 15パズル | A\* を深さ反復で。空間 O(d) |
| ビームサーチ | × | × | O(k·b·d) | 系列/グリッド | 各層で上位 k 個だけ残す。貪欲と BFS の中間 |

> **観察ポイント**：同じ迷路で「展開ノード数」を比べると、UCS(≈BFS)＝一様に多く展開、
> 貪欲＝少ないが非最適、A\*＝最適かつ少数、という三者関係が数字で見える。

### C. 動的計画法（DP）

| 手法 | 最適 | 計算量 | デモ | 実装メモ |
|------|------|--------|------|----------|
| 最短路 DP | ○ | O(V+E)(DAG) | グリッド最短路の表 | 部分問題＝各マスまでの最小コスト |
| 0/1 ナップサック | ○ | O(nW) 擬多項式 | 表が埋まる様子 | `dp[i][w]`。重複部分問題をメモ化 |
| 編集距離 / LIS | ○ | O(nm) / O(n log n) | 表 / 列 | 文字列・列の古典 |

### D. 系統的最適化・枝刈り

| 手法 | 最適 | 計算量 | デモ | 実装メモ |
|------|------|--------|------|----------|
| バックトラッキング | ○(全列挙) | 最悪 指数 | N-Queens 配置 | 矛盾したら即戻る |
| CSP(制約充足) | ○ | 最悪 指数 | 数独 | 前方チェック・MRV で枝刈り強化 |
| 分枝限定法(B&B) | ○ | 最悪 指数(実用は刈れる) | ナップサック/TSP の木 | 上界・下界で有望でない枝を刈る。**αβ の最適化版** |

### E. ゲーム木（Adversarial）

| 手法 | 最適 | 計算量 | デモ | 実装メモ |
|------|------|--------|------|----------|
| Minimax | ○(完全読み) | O(b^m) | 三目並べの木 | 自分=max, 相手=min |
| Alpha-Beta | ○ | 最良 O(b^(m/2)) | 枝刈りの可視化 | 「勝てない未来」を読まない。手順序が効く |
| MCTS | 近似(anytime) | 試行回数に比例 | 木が有望方向へ伸びる | 選択(UCB)→展開→playout→逆伝播。AlphaGo で有名 |

> **橋渡し**：MCTS の「選択」は多腕バンディットの UCB そのもの（J 参照）。
> Alpha-Beta と分枝限定法は「枝刈り」という同じ思想の別ドメイン版。

### F. 局所探索・メタヒューリスティクス

| 手法 | 最適 | 特徴 | デモ | 実装メモ |
|------|------|------|------|----------|
| 山登り(+再スタート) | × | 近傍で改善方向のみ。局所解で停止 | 地形/TSP | ランダム再スタートで緩和 |
| 焼きなまし(SA) | ○(理論上、超低速冷却で) | 悪化も確率的に許容→局所解脱出 | 地形/TSP | 温度 T を徐々に下げる。`exp(-ΔE/T)` |
| タブーサーチ | × | 直近の手を禁止して巡回を防ぐ | TSP | タブーリストで局所解回避 |
| 遺伝的アルゴリズム(GA) | × | 集団＋交叉＋突然変異 | 目標へ収束/TSP | 選択→交叉→変異→反復 |
| 粒子群(PSO) | × | 群が自他の最良に引かれる | 地形 | 速度更新式。連続最適化向け |

### G. 確率的手法（Stochastic）

| 手法 | 用途 | 誤差/収束 | デモ | 実装メモ |
|------|------|-----------|------|----------|
| モンテカルロ | 数値積分・確率推定 | 誤差 ~ 1/√n | π のランダム点推定 | 大数の法則 |
| MCMC(Metropolis) | 分布からのサンプリング | 定常分布へ収束 | 分布をサンプルが埋める | 提案→受理率で採否。ベイズ推論・統計物理 |

### H. 勾配ベース最適化（連続・微分可能）

| 手法 | 特徴 | デモ | 実装メモ |
|------|------|------|----------|
| GD / SGD | 傾きに沿って下る | 2D 損失面の軌跡 | `x -= lr·∇f`。局所解には弱い |
| Momentum | 慣性で谷を滑る | 同上（比較） | 速度項を加える |
| RMSProp | 次元ごとに学習率調整 | 同上 | 勾配二乗の移動平均 |
| Adam | Momentum + RMSProp | 同上 | 実務のデフォルト |

### I. モデルベース最適化

| 手法 | 用途 | デモ | 実装メモ |
|------|------|------|----------|
| ベイズ最適化 | 試行回数が高価な最適化 | 1D GP の事後分布＋獲得関数で次点選択 | GP で予測＋不確実性、EI/UCB で「次に試す点」を決める。ハイパラ探索・材料探索 |

### J. 強化学習（RL）

| 手法 | 収束 | デモ | 実装メモ |
|------|------|------|----------|
| 多腕バンディット | 後悔 O(log T)(UCB) | 各アームの推定と後悔曲線 | ε-greedy と UCB を比較。**探索と活用の原型** |
| Q学習(off-policy) | ○(条件付) | gridworld の価値/方策が収束 | `Q ← Q + α(r + γ max Q' − Q)` |
| SARSA(on-policy) | ○(条件付) | 同上（挙動の違い） | 実際に取った行動で更新（安全寄り） |
| (発展) DQN / 方策勾配 | ― | ― | ニューラルネット。Python(torch) 側で別途 |

---

## 6. 例題（環境）

| 例題 | 主に使う系統 | ねらい |
|------|--------------|--------|
| グリッド迷路（重み付き対応） | A, B | 探索の広がり・最適性⇄コストを目で見る |
| TSP（巡回セールスマン） | F, D | 大規模空間で「そこそこ解」を狙う |
| 0/1 ナップサック | C, D | DP と分枝限定法の対比 |
| N-Queens / 数独 | D | バックトラッキングと制約伝播 |
| 三目並べ / Connect-4 | E | 敵対探索と枝刈り |
| gridworld(MDP) | J | 報酬から方策を学ぶ |
| 1D/2D 関数の地形 | F, H, I | 局所解・勾配・獲得関数 |
| 多腕バンディット | J | 探索と活用のトレードオフ |

---

## 7. ロードマップ（バッチ計画）

各バッチは「動くデモ＋短い解説ページ」を単位に積み上げる。前バッチの共通部品を再利用する。

### バッチ 0 ― 基盤
- `core/`：`SearchProblem` などの型、優先度キュー(binary heap)、計測ユーティリティ、trace 再生器
- トップページ（系統の地図＋各デモへのリンク）
- **成果物**：迷路デモ＋地形デモを共通基盤に載せ替え

### バッチ 1 ― グラフ探索を仕上げる（A, B）
- BFS / DFS / IDDFS / UCS / 双方向 / 貪欲 / A\* / 重み付きA\* / ビーム
- **重み付き地形**を導入し Dijkstra と A\* の差を明確化
- **学べること**：最適性⇄コスト、ヒューリスティックの効き

### バッチ 2 ― 最適化とメタヒューリスティクス（F, H）
- 山登り / 焼きなまし / タブー / GA / PSO / 勾配各種（GD/Momentum/RMSProp/Adam）
- 地形と TSP を共通の `OptimizationProblem` に載せる
- **学べること**：局所解に捕まる/抜ける、集団による探索

### バッチ 3 ― 探索と活用の一本道（J, E）
- 多腕バンディット(ε-greedy/UCB) → **MCTS** → 三目並べ(Minimax + Alpha-Beta)
- Q学習 / SARSA を gridworld で
- **学べること**：探索と活用、枝刈り、価値の学習

### バッチ 4 ― 系統的最適化・DP（C, D）
- ナップサック DP、分枝限定法、N-Queens / 数独（バックトラッキング＋制約伝播）
- **学べること**：記憶と再利用、上界下界による枝刈り

### バッチ 5 ― 確率的・モデルベース（G, I）
- モンテカルロ(π)、MCMC(Metropolis)、ベイズ最適化(1D GP)
- **学べること**：ランダム性の活用、少数試行での最適化

### (任意) バッチ 6 ― 深いRL（Python 併設）
- DQN・方策勾配（torch）。TS では扱いにくい領域だけ Python で

---

## 8. 推奨リポジトリ構成

```
search-algorithms-playground/
├─ README.md
├─ docs/
│  └─ roadmap.md                # 本ファイル
├─ src/
│  ├─ core/                     # 型・IF・優先度キュー・計測・trace
│  │  ├─ types.ts
│  │  ├─ priorityQueue.ts
│  │  └─ metrics.ts
│  ├─ problems/                 # 例題（環境）
│  │  ├─ grid.ts  tsp.ts  knapsack.ts  nqueens.ts
│  │  ├─ ticTacToe.ts  gridworld.ts  landscape.ts  bandit.ts
│  ├─ algorithms/
│  │  ├─ uninformed/            # bfs dfs iddfs ucs bidirectional
│  │  ├─ informed/              # greedy astar weightedAstar idastar beam
│  │  ├─ dp/                    # gridDP knapsackDP editDistance
│  │  ├─ systematic/            # backtracking csp branchAndBound
│  │  ├─ adversarial/           # minimax alphaBeta mcts
│  │  ├─ local/                 # hillClimbing simulatedAnnealing tabu ga pso
│  │  ├─ stochastic/            # monteCarlo mcmc
│  │  ├─ gradient/              # gd momentum rmsprop adam
│  │  ├─ bayesopt/              # gp acquisition
│  │  └─ rl/                    # bandit qlearning sarsa
│  └─ viz/                      # 各デモの描画（canvas / svg / React）
├─ demos/                       # 各アルゴリズム 1 ページ
├─ index.html
├─ package.json
└─ tsconfig.json
```

---

## 9. 学習の推奨順（読む順序）

1. **BFS / DFS**（探索の骨格・open/closed の感覚）
2. **UCS → 貪欲 → A\***（コストとヒューリスティックの導入、最適性⇄コスト）
3. **山登り → 焼きなまし**（局所探索と局所解、探索と活用の入り口）
4. **バンディット/UCB → MCTS**（探索と活用の中核、木への応用）
5. **Minimax → Alpha-Beta**（敵対探索と枝刈り）
6. **DP → 分枝限定法**（記憶と再利用、上下界による枝刈り）
7. **勾配法 → ベイズ最適化**（連続最適化、少数試行）
8. **Q学習 / SARSA**（報酬からの学習、探索と活用の総まとめ）

---

## 10. 技術メモ

- **描画**：軽量デモは vanilla canvas/SVG、状態が多いものは React。共通の trace 再生器で速度調整。
- **乱数**：再現性のためシード付き PRNG（mulberry32 など）を `core` に置く。
- **性能**：優先度キューは binary heap。グリッド規模が大きい場合のみ最適化を検討（学習用途では可読性優先）。
- **テスト**：既知の最適解を持つ小問題（小迷路・小TSP）で最適性・完全性を回帰テスト。
- **命名/コメント**：擬似コードに近い可読性を最優先（教材であることを忘れない）。

---

*このロードマップは実装しながら随時更新する。まずバッチ 0（基盤）＋バッチ 1（グラフ探索）から着手するのが最短。*